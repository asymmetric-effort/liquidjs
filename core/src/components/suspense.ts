import { LIQUID_SUSPENSE_TYPE } from '../shared/types';

/**
 * Suspense component for handling async loading states.
 * Equivalent to React.Suspense.
 */
export const Suspense: symbol = LIQUID_SUSPENSE_TYPE;
