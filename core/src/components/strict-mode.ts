import { LIQUID_STRICT_MODE_TYPE } from '../shared/types';

/**
 * StrictMode enables additional development checks.
 * Equivalent to React.StrictMode.
 */
export const StrictMode: symbol = LIQUID_STRICT_MODE_TYPE;
