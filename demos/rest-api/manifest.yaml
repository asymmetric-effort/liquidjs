name: rest-api
description: Bookmarks CRUD app — SpecifyJS UI + Go REST API
features:
  - fetch API
  - async state management
  - loading/error states
  - CRUD operations
  - custom hooks
  - nginx reverse proxy
containers: 2
ports:
  - "3000:80"
  - "8080 (internal)"
