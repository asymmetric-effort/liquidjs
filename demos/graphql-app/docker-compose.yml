services:
  api:
    build:
      context: ../..
      dockerfile: demos/graphql-app/api/Dockerfile
    expose:
      - "8080"
    restart: unless-stopped

  ui:
    build:
      context: ../..
      dockerfile: demos/graphql-app/ui/Dockerfile
    ports:
      - "3001:80"
    depends_on:
      - api
    restart: unless-stopped
